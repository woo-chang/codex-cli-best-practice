name: Docs QA

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  docs-qa:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run docs QA script
        run: bash scripts/qa-docs.sh
